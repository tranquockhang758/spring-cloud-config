server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

# Bạn có thể thêm security production
management:
  endpoints:
    web:
      exposure:
        include: "*"
